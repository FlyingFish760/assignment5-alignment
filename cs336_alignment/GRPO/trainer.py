import json
import random
from typing import List, Dict


# def get_batch_question(question_dataset: List[Dict[str, str]], batch_size: int):
#     sampled_inds = random.sample(range(len(question_dataset)), k=batch_size)
#     return [question_dataset[i] for i in sampled_inds]

